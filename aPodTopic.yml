apiVersion: v1
Kind: Pod
metadata:
  name: pod-name
  labels: 
    app: myapp
  namespace: some-ns
spec:
  containers:
    - name: myapp-containers
      image: busybox
      command: ['sh', '-c', 'echo hello && sleep 3600']
#eq ==><== 
      command: 
        - 'sh'
        - '-c'
        - 'echo hello && sleep 3600'
    